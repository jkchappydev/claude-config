## 언어 설정
- 응답: 한국어
- 주석: 한국어 (비즈니스 로직만)
- 문서화: 한국어 (IMPORTANT)
- 변수명: 영어

## 코딩 스타일
- 들여쓰기: 2칸
- 네이밍: camelCase

## 아키텍처
- 레이어드 아키텍처 (Controller → Service → Repository)
- DTO 패턴 사용
- 의존성 주입

## 추가 사항
- 에러 핸들링 필수
- DB 트랜잭션 처리
- API 응답 형식 일관성 유지

## 코드 주석 스타일

**코드에 주석을 달아달라고 요청받으면 아래 스타일을 쓴다.
**회사 프로젝트에는 이 스타일을 적용하지 않는다** — 각 회사 프로젝트의 기존 코드 컨벤션을 따를 것.

- 한 줄 요약으로, 명사형으로 끝낸다 ("~함", "~한다" 같은 종결 어미 없이). 예: "DB 연결 인스턴스 import", "공지 전체를 최신순으로 조회"
- 어려운 용어를 무조건 피하지는 않는다 — "싱글톤", "인스턴스" 같은 핵심 용어는 살짝 써도 되지만 과하게 기술적으로 풀어쓰지 않는다.
- 코드 블록/로직 단위마다 그 위에 한 줄 주석을 붙인다 (라인마다 전부 달 필요는 없음)


## 커밋 규칙 (모든 프로젝트 공통)
- 제목 한 줄 위주로 간결하게. 본문은 꼭 필요할 때만 작성
- Co-Authored-By 등 공동작성자 표기 넣지 않음
- **커밋 전 반드시 사용자에게 커밋 메시지 사전 검사를 받을 것**
- 프로젝트별 CLAUDE.md에 별도 커밋 규칙이 있으면 그쪽이 우선
- **커밋할 때마다 `PROGRESS.md`와 오늘 날짜 워크로그를 함께 업데이트할 것** (아래 "PROGRESS.md / 워크로그 관리" 참고)
  - 순서: `daily-worklog` 스킬로 오늘 워크로그를 먼저 작성/갱신한 다음 `git commit`을 실행한다 — SKILL 파일이 있다고 자동 실행되는 게 아니므로, 커밋 요청을 받으면 이 스킬부터 호출한다.

## PROGRESS.md / 워크로그 관리 (모든 프로젝트 공통)

컴퓨터 간 작업 인계 + 그날그날 업무일지, 두 목적을 파일 역할을 나눠서 처리한다. 매 커밋 시 두 파일 모두 갱신한다.

### `PROGRESS.md` (프로젝트 루트) — 인계용 스냅샷
- 날짜를 남기지 않고, 항상 "지금 상태"만 담는 짧은 문서로 유지한다. A 컴퓨터에서 B 컴퓨터로 넘어갈 때 이것만 보면 바로 파악되는 게 목적
- 구성은 아래로 고정한다
  1. 최상단에 `# 진행상황`과 문서 목적을 설명하는 인용구(`>`) 유지
  2. `## 완료된 작업` — 끝난 작업을 한 줄(또는 하위 불릿)로 누적. 더 이상 참고할 필요 없는 항목은 정리 시점에 지운다
  3. `## 다음 진행 예정` — 아직 안 끝났거나 다음에 이어서 할 작업. 어디까지 했는지, 왜 그런 결정을 했는지(논의 결과·전환 이유 등)까지 하위 불릿으로 남긴다
- 커밋마다: 이번 커밋으로 완료된 항목은 "완료된 작업"으로 옮기고, 새로 생긴 다음 할 일은 "다음 진행 예정"에 반영한다

### `docs/worklog/YYYY-MM-DD-{사용자ID}.md` — 날짜별 업무일지
- 프로젝트 루트에 `docs/worklog/` 폴더가 없으면 새로 만든다
- 파일명은 날짜와 작업자별로 나눈다 (`docs/worklog/2026-07-16-jkchappy.md` 형식) — 여러 사람이 같은 프로젝트에 워크로그를 쓸 때 충돌하지 않도록 함
- 작성자 식별, 작업 단위 정리(간결화·커밋 클러스터링), 정보 출처 우선순위 등 상세 규칙은 `daily-worklog` 스킬을 따른다
- 지난 날짜 파일은 수정하지 않는다 — 그날 기록은 그날 파일에 고정
- **예외: `~/.claude`(전역) 자체에는 날짜별 업무일지를 두지 않는다.** 일별 워크로그는 각 프로젝트에서만 관리하고, 전역에는 각 프로젝트의 워크로그를 모은 **주간 워크로그**(`~/.claude/worklog-weekly/YYYY-Www.md`, `worklog-weekly-review` 스킬)만 둔다. `PROGRESS.md`는 전역에도 동일하게 유지한다.

## Git 워크트리 / 브랜치 워크플로우 (모든 프로젝트 공통)
- 사용자의 평소 작업 방식: Claude가 기능 구현을 끝내면, 사용자가 직접 해당 개발 브랜치로 전환해서 구현 내용을 테스트하고, 만족스러우면 release(또는 main)에 PR로 병합한다. 기능 구현은 격리된 git worktree에서 진행하는 걸 선호한다.
- 다만 git은 같은 브랜치를 worktree와 메인 체크아웃 두 곳에서 동시에 체크아웃할 수 없다 — worktree 안에서 브랜치를 만들어 두면, 메인 체크아웃(IntelliJ 등)에서 같은 브랜치로 전환하려 할 때 "already used by worktree" 오류가 난다.
- 구현 완료 후 사용자가 테스트할 때는 **브랜치 전환이 아니라, worktree 폴더 자체를 IntelliJ 등에서 별도 프로젝트(새 창)로 여는 방식을 기본으로 안내한다** — 이러면 브랜치 충돌 없이 바로 그 브랜치 상태로 접근/실행/디버깅할 수 있다. 구현 완료 보고 시 이 방법(정확한 worktree 경로 포함)을 먼저 제시한다.
- 그래도 사용자가 메인 체크아웃에서 브랜치를 직접 전환해 쓰고 싶어하면, 대안으로 `git worktree remove <path>`(브랜치는 보존하고 worktree 등록만 해제)를 안내한다. 이 명령은 Claude가 worktree 세션 안에서 직접 실행할 수 없으므로(harness가 다른 체크아웃을 대상으로 한 git 작업을 차단함) 사용자가 메인 체크아웃 터미널에서 직접 실행해야 한다.

## 애플리케이션 실행/종료 (모든 프로젝트 공통)
- 사용자는 애플리케이션(백엔드 서비스 등) 기동/종료를 IntelliJ에서 직접 한다.
- Claude가 임의로 애플리케이션을 시작하거나 종료하지 않는다 (bootRun, taskkill/kill 등 포함, 자율 루프 점검 중 백그라운드로 띄우는 것도 포함) — 필요하면 사용자에게 직접 기동/종료해달라고 요청한다.
- 예외: 사용자가 "띄워줘", "꺼줘", "테스트해줘" 등으로 명시적으로 요청한 경우에만 직접 수행한다.

## 로컬 전용 참고사항
이 기기에 `CLAUDE.local.md`가 있으면 그 내용도 함께 참고할 것.
